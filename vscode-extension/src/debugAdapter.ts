/**
 * Debug Adapter entry point
 * Starts the debug adapter session
 */

import { HopeIDEDebugSession } from './debugAdapter/hopeideDebugAdapter';

// Start debug adapter
HopeIDEDebugSession.run(HopeIDEDebugSession);
